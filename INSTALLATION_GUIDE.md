# AI Creator Studio - คู่มือการติดตั้งบน Android

## 🚀 วิธีที่ 1: ใช้ Expo Go (ทดสอบทันที - แนะนำ)

### ขั้นตอนที่ 1: ติดตั้ง Expo Go บนมือถือ

1. เปิด **Google Play Store** บนมือถือ Android
2. ค้นหา **"Expo Go"**
3. ติดตั้งแอพ (ฟรี)

### ขั้นตอนที่ 2: เปิด Terminal/Command Prompt บนคอมพิวเตอร์

```bash
cd /home/ubuntu/ai-creator-studio
```

### ขั้นตอนที่ 3: เริ่ม Dev Server

```bash
pnpm dev
```

**ผลลัพธ์ที่คุณจะเห็น:**
```
Expo Go app is ready at http://localhost:19000
Scan the QR code below with Expo Go app to open your project
```

### ขั้นตอนที่ 4: สแกน QR Code

1. เปิด **Expo Go** บนมือถือ
2. แตะ **"Scan QR code"**
3. ชี้กล้องไปที่ QR code ที่ปรากฏใน Terminal
4. รอให้แอพโหลด (ประมาณ 30-60 วินาที)

### ✅ เสร็จแล้ว!

แอพ **AI Creator Studio** จะเปิดขึ้นมาบนมือถือของคุณ

---

## 🔧 วิธีที่ 2: Build APK (ติดตั้งแบบถาวร)

### ขั้นตอนที่ 1: ติดตั้ง EAS CLI

เปิด Terminal/Command Prompt และรันคำสั่ง:

```bash
npm install -g eas-cli
```

### ขั้นตอนที่ 2: ไปที่โปรเจกต์

```bash
cd /home/ubuntu/ai-creator-studio
```

### ขั้นตอนที่ 3: ล็อกอิน Expo (ครั้งแรก)

```bash
eas login
```

**หมายเหตุ:** ถ้ายังไม่มีบัญชี Expo ให้สร้างที่ https://expo.dev

### ขั้นตอนที่ 4: Build APK

```bash
eas build --platform android
```

**ระหว่างการ Build:**
- ระบบจะถามคำถามเกี่ยวกับการตั้งค่า
- ตอบ "Yes" สำหรับคำถามส่วนใหญ่
- รอจนกว่า build เสร็จ (ประมาณ 10-15 นาที)

### ขั้นตอนที่ 5: ดาวน์โหลด APK

1. เมื่อ build เสร็จ คุณจะได้ลิงก์ดาวน์โหลด
2. คลิกที่ลิงก์เพื่อดาวน์โหลด APK
3. APK จะบันทึกลงใน **Downloads** folder

### ขั้นตอนที่ 6: ติดตั้ง APK บนมือถือ

**วิธี A: ใช้ USB Cable**

```bash
adb install Downloads/app-release.apk
```

**วิธี B: ส่งไฟล์ไปยังมือถือ**

1. ส่ง APK ไปยังมือถือผ่าน:
   - Email
   - WhatsApp
   - Google Drive
   - Bluetooth

2. เปิดไฟล์ APK บนมือถือ
3. แตะ **"Install"**
4. รอให้ติดตั้งเสร็จ

### ✅ เสร็จแล้ว!

แอพ **AI Creator Studio** จะติดตั้งบนมือถือของคุณ

---

## 📊 เปรียบเทียบ 2 วิธี

| เกณฑ์ | Expo Go | APK |
|------|---------|-----|
| **ความเร็ว** | ⚡ เร็วมาก (30 วินาที) | ⏱️ ช้า (10-15 นาที) |
| **ความยุ่งยาก** | ง่าย | ปานกลาง |
| **ติดตั้งแบบถาวร** | ❌ ไม่ได้ (ต้อง Expo Go) | ✅ ได้ |
| **ใช้งานปกติ** | ✅ ได้ | ✅ ได้ |
| **ทดสอบเร็ว** | ✅ ดีที่สุด | ❌ ช้า |
| **ไม่ต้องอินเทอร์เน็ต** | ❌ ต้องระหว่างเปิด | ✅ ไม่ต้อง |

---

## 🆘 ปัญหาที่อาจเจอ

### ปัญหา: "Cannot connect to Expo"

**แก้ไข:**
1. ตรวจสอบการเชื่อมต่อ WiFi บนมือถือ
2. ตรวจสอบว่า WiFi เดียวกับคอมพิวเตอร์
3. ลองรัน `pnpm dev` อีกครั้ง

### ปัญหา: "QR code not scanning"

**แก้ไข:**
1. ทำให้ QR code ชัดเจนขึ้น
2. ลองใช้ Expo Go ในการสแกน
3. ลองใช้ลิงก์แทน QR code

### ปัญหา: "Build failed"

**แก้ไข:**
1. ตรวจสอบ Node.js version: `node --version` (ต้อง >= 16)
2. ล้าง cache: `pnpm store prune`
3. ลบ node_modules: `rm -rf node_modules && pnpm install`

### ปัญหา: "APK installation failed"

**แก้ไข:**
1. ตรวจสอบพื้นที่ว่างบนมือถือ (ต้อง >= 100 MB)
2. ลบแอพเวอร์ชันเก่า (ถ้ามี)
3. เปิด "Unknown sources" ในการตั้งค่า (Settings > Security)
4. ลองติดตั้ง APK ใหม่

### ปัญหา: "Permission denied"

**แก้ไข:**
1. ตรวจสอบ Android SDK permissions
2. ใช้ `sudo` ถ้าจำเป็น:
   ```bash
   sudo adb install app-release.apk
   ```

---

## 📱 ข้อกำหนดของมือถือ

- **Android Version:** 5.0 (API 21) ขึ้นไป
- **พื้นที่ว่าง:** อย่างน้อย 100 MB
- **RAM:** อย่างน้อย 2 GB
- **Internet:** ต้องสำหรับ Expo Go, ไม่ต้องสำหรับ APK

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Expo Go - Google Play Store](https://play.google.com/store/apps/details?id=host.exp.exponent)
- [Expo Documentation](https://docs.expo.dev)
- [EAS Build Guide](https://docs.expo.dev/build/introduction/)
- [Android SDK Setup](https://developer.android.com/studio)

---

## 💡 เคล็ดลับ

1. **ใช้ Expo Go ก่อน** - เพื่อทดสอบแอพเร็ว
2. **Build APK ทีหลัง** - เมื่อแน่ใจว่าทำงานได้
3. **เก็บ APK ไว้** - เพื่อติดตั้งใหม่ได้ในอนาคต
4. **ตรวจสอบ WiFi** - ต้องใช้ WiFi เดียวกันสำหรับ Expo Go

---

## 📞 ต้องการความช่วยเหลือ?

ถ้าคุณเจอปัญหาหรือต้องการคำแนะนำ โปรดติดต่อ:
- **Email:** support@aicreatostudio.com
- **Documentation:** https://docs.expo.dev
- **Community:** https://forums.expo.dev

---

## ✨ สำหรับผู้ใช้ที่ไม่มี Terminal/Command Prompt

ถ้าคุณไม่คุ้นเคยกับ Terminal ให้ทำตามนี้:

### Windows:
1. กด `Win + R`
2. พิมพ์ `cmd` และกด Enter
3. พิมพ์คำสั่ง

### Mac:
1. เปิด **Spotlight** (Cmd + Space)
2. พิมพ์ `terminal` และกด Enter
3. พิมพ์คำสั่ง

### Linux:
1. กด `Ctrl + Alt + T`
2. พิมพ์คำสั่ง

---

**ขอให้สนุกกับการใช้ AI Creator Studio! 🎉**
