# AI Creator Studio - Setup Instructions

## 📋 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: ดาวน์โหลดโปรเจกต์

```bash
# ดาวน์โหลดโปรเจกต์จาก:
# /home/ubuntu/ai-creator-studio
```

### ขั้นตอนที่ 2: ติดตั้ง Node.js

**ดาวน์โหลดจาก:** https://nodejs.org/

**ตรวจสอบการติดตั้ง:**
```bash
node --version  # ต้อง >= 16
npm --version
```

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

```bash
cd /home/ubuntu/ai-creator-studio
npm install
# หรือ
pnpm install
```

### ขั้นตอนที่ 4: ติดตั้ง EAS CLI

```bash
npm install -g eas-cli
```

### ขั้นตอนที่ 5: ล็อกอิน Expo

```bash
eas login
```

**ใส่ข้อมูล:**
- Email: `maemb3703@gmail.com`
- Password: `Mm0810583703`

### ขั้นตอนที่ 6: Build APK

```bash
eas build --platform android
```

---

## 🎯 ขั้นตอนต่อไป

1. **รอให้ build เสร็จ** (10-15 นาที)
2. **ดาวน์โหลด APK** จากลิงก์ที่ได้
3. **ติดตั้งบนมือถือ** ตามคำแนะนำใน INSTALL_WITHOUT_PC.md

---

## 📁 ไฟล์ที่สำคัญ

| ไฟล์ | คำอธิบาย |
|-----|---------|
| `QUICK_BUILD_GUIDE.md` | คู่มือ build APK ง่ายๆ |
| `INSTALL_WITHOUT_PC.md` | คู่มือติดตั้งบนมือถือ |
| `EXPO_CREDENTIALS.txt` | ข้อมูล Expo Account |
| `app.config.ts` | การตั้งค่า Expo |
| `eas.json` | การตั้งค่า EAS Build |
| `package.json` | Dependencies |

---

## 🆘 ปัญหาที่อาจเจอ

### "npm: command not found"
- ติดตั้ง Node.js ใหม่ จาก https://nodejs.org/

### "eas: command not found"
- รัน: `npm install -g eas-cli`

### "Build failed"
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ลองรัน: `eas build --platform android` อีกครั้ง

---

## 📞 ต้องการความช่วยเหลือ?

- **Expo Docs:** https://docs.expo.dev
- **EAS Build:** https://docs.expo.dev/build/introduction/
- **Support:** https://forums.expo.dev

---

**ขอให้สำเร็จ! 🎉**
