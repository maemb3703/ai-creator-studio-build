# AI Creator Studio - คู่มือ Build APK ง่ายๆ

## 🚀 วิธีสร้าง APK (ง่ายที่สุด)

### ขั้นตอนที่ 1: เตรียมข้อมูล

**ข้อมูลที่คุณมี:**
- Email: `maemb3703@gmail.com`
- Password: `Mm0810583703`
- Expo Account: สร้างแล้ว

### ขั้นตอนที่ 2: ใช้ Expo Cloud (แนะนำ - ไม่ต้องติดตั้ง Android SDK)

**ให้เพื่อนหรือใครสักคนที่มีคอมพิวเตอร์ทำตามนี้:**

```bash
# 1. ไปที่โปรเจกต์
cd /home/ubuntu/ai-creator-studio

# 2. ติดตั้ง EAS CLI
npm install -g eas-cli

# 3. ล็อกอิน Expo
eas login
# ใส่ Email: maemb3703@gmail.com
# ใส่ Password: Mm0810583703

# 4. Build APK
eas build --platform android

# 5. รอจนกว่า build เสร็จ (ประมาณ 10-15 นาที)
# 6. คัดลอกลิงก์ดาวน์โหลด APK
```

### ขั้นตอนที่ 3: ดาวน์โหลด APK

1. เมื่อ build เสร็จ คุณจะได้ลิงก์
2. ดาวน์โหลด APK
3. ส่ง APK ให้คุณ (ผ่าน WhatsApp, Email, Google Drive, etc.)

### ขั้นตอนที่ 4: ติดตั้งบนมือถือ

1. เปิด Browser บนมือถือ
2. ดาวน์โหลด APK
3. ไปที่ Settings > Security > เปิด "Unknown sources"
4. เปิด File Manager > Downloads
5. แตะไฟล์ APK
6. แตะ "Install"
7. รอให้เสร็จ
8. แตะ "Open"

---

## 💻 ถ้าคุณมีคอมพิวเตอร์

### ขั้นตอนที่ 1: ติดตั้ง Node.js

ดาวน์โหลดจาก: https://nodejs.org/
- ติดตั้ง LTS version (ต้อง >= 16)

### ขั้นตอนที่ 2: เปิด Terminal/Command Prompt

**Windows:**
- กด `Win + R`
- พิมพ์ `cmd` และกด Enter

**Mac:**
- กด `Cmd + Space`
- พิมพ์ `terminal` และกด Enter

**Linux:**
- กด `Ctrl + Alt + T`

### ขั้นตอนที่ 3: ติดตั้ง EAS CLI

```bash
npm install -g eas-cli
```

### ขั้นตอนที่ 4: ไปที่โปรเจกต์

```bash
cd /home/ubuntu/ai-creator-studio
```

### ขั้นตอนที่ 5: ล็อกอิน Expo

```bash
eas login
```

**ใส่ข้อมูล:**
- Email: `maemb3703@gmail.com`
- Password: `Mm0810583703`

### ขั้นตอนที่ 6: Build APK

```bash
eas build --platform android
```

### ขั้นตอนที่ 7: รอจนกว่า Build เสร็จ

- ระหว่างการ build ระบบจะถามคำถาม
- ตอบ "Yes" สำหรับคำถามส่วนใหญ่
- รอประมาณ 10-15 นาที

### ขั้นตอนที่ 8: ดาวน์โหลด APK

- เมื่อ build เสร็จ คุณจะได้ลิงก์
- ดาวน์โหลด APK

### ขั้นตอนที่ 9: ติดตั้งบนมือถือ

**วิธี A: ใช้ USB Cable**

```bash
adb install app-release.apk
```

**วิธี B: ส่งไฟล์ไปยังมือถือ**

1. ส่ง APK ไปยังมือถือ
2. เปิดไฟล์ APK
3. แตะ "Install"

---

## 🆘 ปัญหาที่อาจเจอ

### ปัญหา: "Build failed"

**แก้ไข:**
```bash
# ล้าง cache
npm cache clean --force

# ลบ node_modules
rm -rf node_modules

# ติดตั้งใหม่
npm install
```

### ปัญหา: "Cannot login"

**แก้ไข:**
1. ตรวจสอบ Email: `maemb3703@gmail.com`
2. ตรวจสอบ Password: `Mm0810583703`
3. ลองล็อกอิน Expo ใหม่:
   ```bash
   eas logout
   eas login
   ```

### ปัญหา: "Build timeout"

**แก้ไข:**
1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ลองใช้ `--local` flag:
   ```bash
   eas build --platform android --local
   ```

### ปัญหา: "APK installation failed"

**แก้ไข:**
1. ตรวจสอบพื้นที่ว่าง (>= 100 MB)
2. เปิด "Unknown sources" ในการตั้งค่า
3. ลบแอพเวอร์ชันเก่า (ถ้ามี)

---

## 📊 ระยะเวลา

| ขั้นตอน | ระยะเวลา |
|--------|---------|
| ติดตั้ง EAS CLI | 2-3 นาที |
| Build APK | 10-15 นาที |
| ดาวน์โหลด APK | 2-5 นาที |
| ติดตั้งบนมือถือ | 1-2 นาที |
| **รวม** | **15-25 นาที** |

---

## 📱 ข้อกำหนดของมือถือ

- **Android Version:** 5.0 (API 21) ขึ้นไป
- **พื้นที่ว่าง:** อย่างน้อย 100 MB
- **RAM:** อย่างน้อย 2 GB

---

## 💡 เคล็ดลับ

1. **ใช้ Expo Cloud** - ไม่ต้องติดตั้ง Android SDK
2. **ใช้ WiFi** - ดาวน์โหลดเร็วกว่า
3. **เก็บ APK ไว้** - เพื่อติดตั้งใหม่ได้ในอนาคต
4. **ตรวจสอบ Node.js** - ต้อง >= 16

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Node.js Download](https://nodejs.org/)
- [Expo Documentation](https://docs.expo.dev)
- [EAS Build Guide](https://docs.expo.dev/build/introduction/)
- [Android Setup](https://developer.android.com/studio)

---

## 📞 ต้องการความช่วยเหลือ?

ถ้าคุณเจอปัญหาหรือต้องการคำแนะนำ:
- **Email:** support@aicreatostudio.com
- **Expo Support:** https://forums.expo.dev
- **Android Help:** https://support.google.com/android

---

**ขอให้สำเร็จในการ build APK! 🎉**
